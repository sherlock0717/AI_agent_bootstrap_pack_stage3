---
applyTo: "projects/**/*.md,projects/**/*.yaml"
---

Treat files in projects/ as onboarding records for external projects.

Prefer:
- consistency between project manifest, local agents, and README
- preserving the boundary between system root repo and external project repo
- template-driven onboarding over ad hoc edits
